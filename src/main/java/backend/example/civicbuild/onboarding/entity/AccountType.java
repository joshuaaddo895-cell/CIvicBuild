package backend.example.civicbuild.onboarding.entity;

/** Frontend account type chosen during onboarding. Maps to backend {@link backend.example.civicbuild.auth.entity.Role}. */
public enum AccountType {
    customer,
    construction,
    delivery
}
